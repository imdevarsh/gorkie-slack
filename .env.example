# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/server/env.ts"
# should be updated accordingly.

# ----------------------------------------------------------------------------
# Slack Credentials
# ----------------------------------------------------------------------------
# Tip: you can create your Slack app using the provided slack-manifest.json.
# App manifest: ./slack-manifest.json

# Optional: Socket Mode token (requires connections:write scope)
# @docs: https://api.slack.com/apis/connections/socket
SLACK_APP_TOKEN="xapp-your-app-token"

# Bot User OAuth Token from OAuth & Permissions
# @docs: https://api.slack.com/authentication/token-types
SLACK_BOT_TOKEN="xoxb-your-bot-token"

# Signing Secret from Basic Information
SLACK_SIGNING_SECRET="your-signing-secret"

# Enable Socket Mode (true/false). Default: true for local dev.
SLACK_SOCKET_MODE=true

# Channel to add a user to automatically when they mention the bot (optional)
# AUTO_ADD_CHANNEL="C12345678"

# Channel required for usage of Gorkie beyond pings (optional)
# OPT_IN_CHANNEL="C12345678"

# ----------------------------------------------------------------------------
# Rate Limiting / Caching
# ----------------------------------------------------------------------------
# Redis connection string for rate limiting and caching
# @docs: https://redis.io/docs/latest/
REDIS_URL="redis://localhost:6379"

# ----------------------------------------------------------------------------
# AI Inference Credentials
# ----------------------------------------------------------------------------
# Hack Club (primary model provider)
# @docs: https://ai.hackclub.com/
HACKCLUB_API_KEY="sk-hc-your-hackclub-api-key"

# OpenRouter (secondary model provider)
# @docs: https://openrouter.ai/keys
OPENROUTER_API_KEY="sk-or-your-openrouter-api-key"

# ----------------------------------------------------------------------------
# Langfuse (Tracing / Observability)
# ----------------------------------------------------------------------------
# @docs: https://langfuse.com/docs
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_PUBLIC_KEY="pk-lf-..."
# EU region base URL. For US region, use "https://us.cloud.langfuse.com".
LANGFUSE_BASEURL="https://cloud.langfuse.com"

# ----------------------------------------------------------------------------
# Web Search (Exa)
# ----------------------------------------------------------------------------
# @docs: https://exa.ai/docs/home
EXA_API_KEY=""

# ----------------------------------------------------------------------------
# Vercel (Sandbox)
# ----------------------------------------------------------------------------
# Personal Access Token (https://docs.vercel.com/docs/rest-api/reference/welcome#creating-an-access-token)
VERCEL_TOKEN=""

# Team ID (https://vercel.com/docs/accounts#find-your-team-id)
VERCEL_TEAM_ID="team_xyz"

# Project ID (https://vercel.com/docs/project-configuration/general-settings#project-id)
VERCEL_PROJECT_ID="prj_abc"
