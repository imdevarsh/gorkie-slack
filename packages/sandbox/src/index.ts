// @repo/sandbox — the e2b sandbox layer for the harness.
//
// Lands in Phase 2/3: a custom `HarnessV1SandboxProvider` over @e2b/code-interpreter
// (create/resume/pause/destroy), the session lifecycle, and the DB session-file
// mirror that lets a conversation survive a sandbox kill. See ../../REWRITE_PLAN.md §4a.

export {};
