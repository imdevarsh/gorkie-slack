// @repo/ai — the agent core.
//
// Lands in Phase 2: `createPi` + `HarnessAgent` assembly, the unified system
// prompt, and host-executed AI SDK tools. Env keys are exported from `./keys`.
// See ../../REWRITE_PLAN.md §4 / §4a.

export {};
