pre-commit:
  commands:
    ultracite:
      glob: "*.{ts,tsx,js,jsx,json,jsonc}"
      run: bun x ultracite check {staged_files}

post-merge:
  commands:
    install-deps:
      run: bun install

commit-msg:
  commands:
    "lint commit message":
      run: bun commitlint --edit {1}
