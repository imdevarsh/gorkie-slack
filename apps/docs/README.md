# Gorkie Docs

Internal documentation for the Gorkie v2 rewrite.

The content lives in `content/` and is written as MDX. Keep it compact, practical, and tied to the source files that own each behavior.

## Development

```sh
bun run dev
```

Useful checks:

```sh
bun run types:check
bun run build
```
